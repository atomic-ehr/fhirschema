{
  "name": "fhirschema",
  "version": "0.0.1",
  "description": "FHIRSchema converter and validator",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}